import React from "react";

const H2 = ({ children }) => {
    return (
        <h2 className="text-3xl font-semibold text-iapm-black leading-relaxed">
            {children}
        </h2>
    );
};

export default H2;
